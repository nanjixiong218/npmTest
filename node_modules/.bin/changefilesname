#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../changefilesname/bin/changefilesname" "$@"
  ret=$?
else 
  node  "$basedir/../changefilesname/bin/changefilesname" "$@"
  ret=$?
fi
exit $ret
